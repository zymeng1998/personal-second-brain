{
  "name": "@sb/fact-store",
  "version": "0.0.0",
  "private": true,
  "description": "L3 structured facts as a rebuildable SQLite projection. SB-035: ADD-only addFact(). Domain-neutral.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "node --import tsx --test test/add-fact.test.ts test/supersede-fact.test.ts test/query.test.ts"
  },
  "dependencies": {
    "@sb/event-log": "workspace:*",
    "@sb/interfaces": "workspace:*",
    "@sb/memory-kernel": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
