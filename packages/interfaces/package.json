{
  "name": "@sb/interfaces",
  "version": "0.0.0",
  "private": true,
  "description": "The stable boundary: TypeScript types + operation contracts aligned to the JSON schemas. Domain-neutral.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/scope.test.ts test/grants.test.ts test/grant-config.test.ts"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "typescript": "^5.4.0",
    "tsx": "^4.0.0"
  }
}
