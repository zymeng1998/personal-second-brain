{
  "name": "@sb/note-vault",
  "version": "0.0.0",
  "private": true,
  "description": "Markdown + YAML-frontmatter vault. SB-011: low-level raw (L0) write primitive. Domain-neutral.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "test": "node --import tsx --test test/raw-note-writer.test.ts"
  },
  "dependencies": {
    "@sb/interfaces": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
