# obsidian-helper (app)

Optional companion that surfaces core capabilities inside Obsidian (capture, distillation prompts).
**Post-MVP (Phase 5).** Obsidian is an optional surface, not a dependency (ADR-003).

- Status: **Phase 0 — no code.**
- Calls `packages/interfaces` like any surface — no special access to internals.
