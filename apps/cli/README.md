# cli (app)

The **MVP surface**. Command-line capture into the vault + event log, plus read/list and the
human-confirmed distillation workflow.

- Status: **Phase 0 — no code.** Implemented in Phase 1.
- Calls `packages/interfaces` only (no direct vault/DB access).
- Planned commands (MVP): `capture`, `note get`, `note list`, `distill` (proposes; human confirms).
