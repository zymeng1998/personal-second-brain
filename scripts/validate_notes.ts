/**
 * validate_notes.ts — STUB (Phase 0). No implementation yet.
 *
 * Phase 1 will: walk the vault and validate each note's YAML frontmatter against
 * schemas/markdown/frontmatter.schema.json, reporting violations. Read-only; never mutates notes.
 */
console.error("[validate_notes] Not implemented in Phase 0. See docs/planning/mvp_scope.md.");
process.exit(1);
