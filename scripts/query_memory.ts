/**
 * query_memory.ts — STUB (Phase 0). No implementation yet.
 *
 * Phase 3 will: query the retrieval sidecar (FTS/vector/graph/temporal) via the @sb/retrieval facade
 * and print results with source_ref provenance. Read-only.
 *
 * See docs/architecture/retrieval_strategy.md.
 */
console.error("[query_memory] Not implemented in Phase 0 (retrieval is Phase 3).");
process.exit(1);
