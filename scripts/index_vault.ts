/**
 * index_vault.ts — STUB (Phase 0). No implementation yet.
 *
 * Phase 3 will: invoke the Python retrieval sidecar (stdio JSON/JSONL) to (re)build the L4 indexes
 * under indexes/ from L0-L2 + events. Indexes are disposable; rebuilding must be lossless.
 *
 * See docs/architecture/sidecar_contract.md and docs/architecture/retrieval_strategy.md.
 */
console.error("[index_vault] Not implemented in Phase 0 (retrieval is Phase 3).");
process.exit(1);
